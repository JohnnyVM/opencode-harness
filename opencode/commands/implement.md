---
description: Resolve and implement one approved open GitHub issue package
agent: implementation-orchestrator
subtask: false
---

Resolve and execute the single durable GitHub Issue Reference supplied as the
complete command argument below:

$ARGUMENTS

Forward that argument as the issue-reference input. Do not reinterpret it as a
copied package or accept additional references. Missing, malformed, multiple,
or invalid references must fail through the Implementation Orchestrator's
normal durable package-intake contract.
